{
  "name": "@zuplo/formdata-to-string",
  "version": "1.1.0",
  "description": "Transform a FormData object into a raw string",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .js,.ts",
    "prebuild": "rm -rf dist/",
    "prepack": "npm run build",
    "pretest": "npm run lint",
    "prettier": "prettier --list-different --write \"./**/**.{js,ts,md}\"",
    "test": "vitest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:zuplo/formdata-to-string.git"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/zuplo/formdata-to-string/issues"
  },
  "homepage": "https://github.com/zuplo/formdata-to-string#readme",
  "dependencies": {
    "undici": "^5.24.0"
  },
  "devDependencies": {
    "@readme/eslint-config": "^12.2.0",
    "@vitest/coverage-v8": "^0.34.3",
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2",
    "vitest": "^0.34.3"
  },
  "browserslist": [
    "last 2 versions"
  ],
  "prettier": "@readme/eslint-config/prettier"
}
